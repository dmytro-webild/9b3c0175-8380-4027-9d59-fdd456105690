import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Montserrat } from "next/font/google";



export const metadata: Metadata = {
  title: 'SILVERPATH SALES LTD | Premium Home Lifestyle Retail UK',
  description: 'SILVERPATH SALES LTD offers curated home lifestyle products, décor, and smart home solutions delivered across the UK.',
  openGraph: {
    "title": "SILVERPATH SALES LTD | Premium Home Lifestyle",
    "description": "Curated home lifestyle products and smart home gadgets.",
    "siteName": "SILVERPATH SALES LTD"
  },
};

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${montserrat.variable} antialiased`}>
          
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
